# Promethist MCP server config. Copy to .env.local if you want to override defaults.
# You do NOT need to set anything here — the plugin logs you in via your browser on first use.
PROMETHIST_BASE_URL=https://preview.eu.promethist.ai
# Optional manual overrides (leave blank to use browser login):
PROMETHIST_TOKEN=
PROMETHIST_COOKIE=
